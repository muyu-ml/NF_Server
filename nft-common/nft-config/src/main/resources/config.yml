spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${nft.nacos.server.url}
      config:
        server-addr: ${nft.nacos.server.url}
        file-extension: properties
        name: ${spring.application.name}